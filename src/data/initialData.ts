import { ReferralCase, StudentNEAE } from '../types';

// Banco de datos inicial completamente limpio (0 alumnos ficticios)
// Todos los expedientes y censos serán dados de alta de forma real y confidencial por el personal autorizado.
export const INITIAL_CASES: ReferralCase[] = [];

export const INITIAL_STUDENTS_NEAE: StudentNEAE[] = [];
